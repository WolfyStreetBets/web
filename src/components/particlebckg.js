import React from "react";
import Particles from 'react-particles-js';
import ParticlesConfig from '../config/particle-config.js';

const Particlebckg = () => {
    return (
          <Particles params={ParticlesConfig}></Particles>
      );
}

export default Particlebckg;